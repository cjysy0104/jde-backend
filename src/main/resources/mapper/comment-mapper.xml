<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.kh.jde.comment.model.dao.CommentMapper">
	
	<!-- 리뷰 존재 확인 -->
	<select id="existsComment" 
			parameterType="Long"
			resultType="CommentDTO">
		SELECT
				  TC.COMMENT_NO commentNo
				, TC.REVIEW_NO reviewNo
				, TC.MEMBER_NO memberNo
				, TC.CONTENT content
				, TC.COMMENT_DATE commentDate
		        , (
		            SELECT COUNT(*)
		            FROM TB_COMMENT_LIKE TCL
		            WHERE TCL.COMMENT_NO = TC.COMMENT_NO
		          ) likeCount
		        , TC.STATUS status
		    FROM
		          TB_COMMENT TC
		    WHERE
		          TC.COMMENT_NO = #{commentNo}
    </select>
    
    <select id="getCommentList"
	        parameterType="Long"
	        resultType="CommentDTO">

		SELECT
				  TC.COMMENT_NO commentNo
				, TC.REVIEW_NO reviewNo
				, M.NICKNAME nickname
				, TC.CONTENT content
				, TC.COMMENT_DATE commentDate
		        , NVL(CNT.LIKE_COUNT, 0) likeCount
	    FROM
				TB_COMMENT TC
		JOIN
				TB_MEMBER M ON (M.MEMBER_NO = TC.MEMBER_NO)
		LEFT
		JOIN
		    	(
		    		SELECT
		    				  COMMENT_NO
		    				, COUNT(*) LIKE_COUNT
		    		FROM
		    				TB_COMMENT_LIKE
		    		GROUP
		    		BY
		    				COMMENT_NO
		    	)	CNT ON (CNT.COMMENT_NO = TC.COMMENT_NO)
		WHERE
				TC.REVIEW_NO = #{reviewNo}
		AND
				TC.STATUS = 'Y'
		ORDER
		BY
				TC.COMMENT_DATE ASC
	
	</select>

	<insert id="create"
			parameterType="CommentVO">
		INSERT
		INTO
				TB_COMMENT
				(
				  COMMENT_NO
				, MEMBER_NO
				, REVIEW_NO
				, CONTENT
				, COMMENT_DATE
				, STATUS
				)
		VALUES
				(
				  SEQ_COMMENT.NEXTVAL
				, #{memberNo}
				, #{reviewNo}
				, #{content}
				, SYSDATE
				, DEFAULT
				)
	</insert>


	<update id="deleteById"
			parameterType="Long">
		UPDATE
				TB_COMMENT
		SET
				STATUS='N'
		WHERE
				COMMENT_NO = #{commentNo}
		AND
				STATUS='Y'
	</update>


	<update id="update"
			parameterType="CommentUpdateDTO">
		UPDATE
				TB_COMMENT
		SET
				CONTENT=#{content}
		WHERE
				COMMENT_NO = #{commentNo}
		AND
				STATUS='Y'
	</update>

<<<<<<< HEAD
=======




>>>>>>> 40404db1df91fe299e2a98efcc66e74b0ff0a021
</mapper>